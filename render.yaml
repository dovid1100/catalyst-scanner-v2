services:
  - type: web
    name: catalyst-scanner-v2
    env: node
    region: oregon
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: POLYGON_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: TZ
        value: America/New_York
    disk:
      name: data
      mountPath: /opt/render/project/src
      sizeGB: 1
